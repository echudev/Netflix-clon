import styles from './CardTags.module.css';

export const CardTags = (props) => {

  return (<div>
    {/* {props.loading ? <div>loading</div>
      :
      <div className={styles.container}>CardTags
        <ul>{props.data.genres.map((data, i) => {
          return (<li key={data.id}>{data.name}</li>)
        })}
        </ul>
      </div>
    } */}
  </div>)
}